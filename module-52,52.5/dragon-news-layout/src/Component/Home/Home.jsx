import PropTypes from 'prop-types'; // ES6

const Home = () => {
    return (
        <div>
            <p>home</p>
        </div>
    );
};

export default Home;

Home.propTypes = {
   props : PropTypes.node
}